function multiplicar(a: number, b: number): number {
    return a * b;
}

function saudacao(nome: string): string {
    return "Olá " + nome;
}

// Exemplos de uso
console.log(multiplicar(5, 3)); // 15
console.log(saudacao("Michael")); // Olá Michael
